apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grafana
  namespace: monitoring
  annotations:
    # cert-manager가 tls.secretName으로 인증서를 발급·갱신
    cert-manager.io/cluster-issuer: raillo-issuer
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - monitoring.raillo.site
      secretName: monitoring-raillo-site-tls
  rules:
    - host: monitoring.raillo.site
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: grafana
                port:
                  number: 3000
