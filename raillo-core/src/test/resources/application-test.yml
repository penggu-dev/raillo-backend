spring:
  config.activate.on-profile: "test"
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
        highlight_sql: true
    open-in-view: false

  batch:
    jdbc:
      initialize-schema: always
    job:
      enabled: false

  mail:
    host: localhost
    port: 3025
    username: testUser
    password: testPassword
    test-connection: false # 테스트 시 메일 연결 확인 비활성화
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: false
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

logging:
  level:
    org.hibernate.SQL: INFO
    org.hibernate.type.descriptor.sql.BasicBinder: INFO
    org.springframework.jdbc.core: INFO
    io.lettuce.core: WARN

jwt:
  secret: "railotestjwtsecretkey2026fordevelopmentandtestingonlylonglonglonglonglonglonglonglonglong"

cors:
  allowed-origins: http://localhost:3000
  allowed-methods: GET, POST, PUT, DELETE
  allowed-headers: Access-Control-Allow-Origin, Content-type, Access-Control-Allow-Headers, Authorization, X-Requested-With

cookie:
  domain: .test

payment:
  toss:
    api:
      base-url: https://api.tosspayments.com
      client-key: test_ck_stub
      secret-key: test_sk_stub
      version: v1

# redis TTL
redis:
  ttl:
    pending-booking: 10m
    pending-booking-member-key: 580s # 유령 인덱스 방지를 위해 주 키의 TTL 설정 값보다 20초 짧게 설정
